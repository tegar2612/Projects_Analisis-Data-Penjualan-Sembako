import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Baca CSV
df = pd.read_csv('data_penjualan_sembako_realistik.csv', delimiter=';')

# Konversi Tanggal
df['Tanggal'] = pd.to_datetime(df['Tanggal'], dayfirst=True)

# Lihat struktur data
df.info()
df.head()
